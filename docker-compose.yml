services:
  ubuntu:
    build:
      context: .
      dockerfile: docker/ubuntu.Dockerfile
    volumes:
      - .:/dotfiles

  fedora:
    build:
      context: .
      dockerfile: docker/fedora.Dockerfile
    volumes:
      - .:/dotfiles

  ubuntu-e2e:
    build:
      context: .
      dockerfile: docker/ubuntu-e2e.Dockerfile

  fedora-e2e:
    build:
      context: .
      dockerfile: docker/fedora-e2e.Dockerfile

  arch-e2e:
    build:
      context: .
      dockerfile: docker/arch-e2e.Dockerfile
