tmux
neovim
neomutt
isync
notmuch
keyutils
# secret-tool lives in libsecret on Fedora, not a separate -tools package.
libsecret
b4
# GnuPG for b4 patch signing; pinentry on Fedora is a single package that
# ships the curses prompt (and the GTK one when the deps are present).
gnupg2
pinentry
gh
fzf
ripgrep
zsh
xclip
wl-clipboard
clang-tools-extra
bear
# Rust: toolchain + rust-analyzer LSP (packaged, since rustup.rs is unreachable
# on hosts without direct internet access)
rust
cargo
rust-analyzer
rust-src
clippy
